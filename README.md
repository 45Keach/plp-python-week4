# Week 3 Assignment on Conditions and Loops

## Grade Reporter & Bug Hunt

- `grade_reporter.py` — Uses a `for` loop and conditions to assign grades, count passes and failures, and calculate the average score.
- `bug_hunt.py` — Fixes three bugs in a `while` loop program and correctly calculates the sum from 1 to 5.

The hardest bug to find in Part B was the `count < 5` condition because the program runs without an error message. I knew something was wrong because the output was not the expected `15`, which showed that the loop was stopping before adding 5.
